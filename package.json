{
  "name": "prospect-me",
  "private": true,
  "type": "module",
  "workspaces": [
    "recon",
    "labs"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "recon": "npm run --silent -w recon recon --",
    "labs": "npm run -w labs --"
  },
  "devDependencies": {
    "railway": "^3.11.0"
  }
}
